Buffer 里的字节 -> socket 输出缓冲区 -> 操作系统 -> 网络

    如果你愿意，我可以继续给你画一版 string -> Buffer -> socket.write -> TCP 的完整数据流
  socket.write 应该是写到 socket 的输入缓冲区吧


• 不是。socket.write() 写的是 socket 的可写方向，也就是发送出去的数据，对应的是输出/发送缓冲
  区，不是输入缓冲区。

  更准确地说：

  - socket.on('data', ...) -> 读的是 接收缓冲区 里的数据

  所以你这条链路应该是：

    -> 操作系统 TCP 发送缓冲区
    -> 网络

  而“输入缓冲区”一般指：

  网络
    -> 操作系统接收缓冲区
    -> Node 触发 data 事件

  一句话总结：

  socket.write 是写出去，不是读进来，所以不是输入缓冲区。


› 再tcp 连接中 接受数据写到 socket 的输入缓冲区，通过 socket.on('data')来获取数据 可以这么理
  解吗


• 可以，这个理解基本对。

  更准确地说是：

  对端发来的 TCP 数据
    -> 先进入操作系统的接收缓冲区
    -> Node 读取后交给 socket
    -> 触发 socket.on('data', ...)

  所以你可以把它理解成：

  - socket 的“输入方向” 接收数据
  - socket.on('data') 是读取收到的数据
  - 数据来源是网络对端，不是你手动写进去的

  只要注意一点：
  data 事件收到的是 Node 已经帮你从底层拿上来的字节块，不一定对应对端一次发送的完整消息。它只
  是 TCP 字节流的一段