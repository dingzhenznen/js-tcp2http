文本文件和二进制文件有什么区别


• 本质上：所有文件底层都是字节。

  区别在于：这些字节能不能按某种字符编码解释成人能读懂的文本。

  文本文件

  比如 .txt、.html、.json、.js：

  hello

  底层其实也是字节：

  68 65 6c 6c 6f

  这些字节可以按 utf8 解码成字符串：

  const text = fs.readFileSync('./a.txt', 'utf8');

  二进制文件

  比如 .png、.jpg、.mp3、.zip：

  89 50 4E 47 ...

  这些字节不是给人直接按文本读的，而是要交给对应格式的解析器：

  - PNG 解码器
  - JPEG 解码器
  - ZIP 解压器
  - MP3 解码器

  读取时通常不传编码：

  const buffer = fs.readFileSync('./a.png');

  否则如果你强行按 utf8 读图片：

  const text = fs.readFileSync('./a.png', 'utf8');

  就可能乱码，甚至破坏数据。

  总结：

  - 文件底层都是字节
  - 文本文件：字节可以按编码转成字符串
  - 二进制文件：字节需要按文件格式解析
  - Node 不传编码读文件 -> Buffer
  - Node 传编码读文件 -> string